import React from "react";
import useTitle from "../../Hooks/useTitle";

const About = () => {
  useTitle("About");
  return (
    <div>
      <h2>LawCorner</h2>
    </div>
  );
};

export default About;
